from setuptools import setup, find_packages

setup(
    name="hoi",
    version="1.0.1",
    description="Hand Object Interaction Detector",
    python_requires=">=3.5.0",
    packages = find_packages(),
)